***REMOVED*** 验证计划：{简述}

> Ticket: {TICKET} | 日期: {YYYY-MM-DD HH:MM} | 状态: ⬜

> 📜 本文件是**可执行验证脚本集**，不是事后文档。写代码前先看它，写完自动跑自测。
> 失败 → 修代码 → 重跑，直到全部通过（Loop Engineering 闭环）。

---

***REMOVED******REMOVED*** 一、数据准备

```sql
-- 准备测试数据
-- SELECT ... FROM t_xxx WHERE ...;
```

***REMOVED******REMOVED*** 二、API/入口验证用例

***REMOVED******REMOVED******REMOVED*** TC-API-01: {正常场景}

```bash
curl -X POST 'http://localhost:8080/api/xxx' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**预期**：HTTP 200 + 响应含字段 `xxx`

***REMOVED******REMOVED******REMOVED*** TC-API-02: {异常场景}

```bash
curl -X POST 'http://localhost:8080/api/xxx' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

**预期**：HTTP 4xx/5xx + 错误信息

***REMOVED******REMOVED*** 三、数据一致性验证

***REMOVED******REMOVED******REMOVED*** 链 1: {数据流描述}

```sql
-- Step 1: 验证上游数据
-- SELECT ... FROM t_xxx WHERE id = 'TEST_001';
-- Step 2: 验证下游数据
-- SELECT ... FROM t_yyy WHERE id = 'TEST_001';
```

**预期**：

***REMOVED******REMOVED*** 四、安全三条件验证（必做，不可跳过）

> 以下来自 02_requirements §五。涉及计费/库存/权限/写操作时每次实施必须全部验证通过。

***REMOVED******REMOVED******REMOVED*** 4.1 退出条件

| ***REMOVED*** | 检查项 | 命令/SQL | 预期 |
|---|--------|---------|------|
| 1 | 重试次数上限生效 | — | N 次后放弃/进死信 |
| 2 | 确定性失败不重试 | — | 4xx 不触发重试 |

***REMOVED******REMOVED******REMOVED*** 4.2 幂等条件

| ***REMOVED*** | 检查项 | 命令/SQL | 预期 |
|---|--------|---------|------|
| 1 | 幂等记录写入 | `SELECT count(*) ...` | ≥ 1 |
| 2 | 重复调用不重复执行 | 同请求调 2 次结果相同 | DB 无重复记录 |

***REMOVED******REMOVED******REMOVED*** 4.3 回滚条件

| ***REMOVED*** | 检查项 | 命令/SQL | 预期 |
|---|--------|---------|------|
| 1 | 结构变更先于代码部署 | — | 失败自动回滚 |
| 2 | 异常时数据不变 | 模拟异常查 DB | 无部分写入 |

***REMOVED******REMOVED*** 五、合规检查

| ***REMOVED*** | 检查项 | 命令/SQL | 预期 |
|---|--------|---------|------|
| 1 | 资源增减对称（涉及库存/额度等资源类） | — | 释放 = 预占 + 扣减 的逆操作 |
| 2 | 事务回滚验证 | 模拟异常 | 数据不变 |
| 3 | 铁律/宪法违规检查 | 对照 `laws.md` + 项目宪法 | 零违规 |

***REMOVED******REMOVED*** 六、合并后对账（代码/配置类置 ✅ 前必填；调研/方法论类对账产物）

> 逐条 AC 核对最终交付，防「验证过了但某链路漏适配」逃逸。合并/交付后填写。

| AC***REMOVED*** | 结论（✅实现 / ⚠️跑偏 / 🔴风险） | 证据（文件 / commit / SQL 结果） |
|-----|:---:|------|
| AC-1 |  |  |

***REMOVED******REMOVED******REMOVED*** 审核签字（责任绑定）

> 审核须独立视角（如对抗审查），签字者对合并验收/发布负责；内容变更后签字失效。

| 项 | 值 |
|------|------|
| 产品/需求视角审核（AC 对账签字） |  |
| 合并验收责任 |  |
| 发布责任（无发布场景 N/A） |  |
| 结论（✅可合并 / ⚠️带风险 / 🔴打回）+ 日期 |  |

***REMOVED******REMOVED*** 七、手工验证（可选）

- [ ]
- [ ]

---

***REMOVED******REMOVED*** Loop Engineering 使用说明

```
┌──────────┐   失败    ┌──────────┐   失败    ┌──────────┐
│ 写代码     │ ──────→ │ 跑验证    │ ──────→ │ 查一致性 │
│          │ ←────── │ §二      │ ←────── │ §三/四   │
└──────────┘  修代码  └──────────┘  修代码  └──────────┘
                                                    │
                                                全部通过
                                                    ↓
                                               ✅ 可提交
```

**AI 使用**：写码前读全部验证用例 → 写码后逐节跑并记录 → 失败修码重跑到全过 → 04_tasks Phase 4 打勾进 Phase 5。
**人工审查**：对照本文件逐项验证，每个勾选项应有通过证据（日志/截图/SQL 结果）。
