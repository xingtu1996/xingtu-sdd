***REMOVED*** 详细设计：{简述}

> Ticket: {TICKET} | 日期: {YYYY-MM-DD HH:MM} | 状态: ⬜

> 📜 **Check**：设计不得违反同目录 [laws.md](./laws.md) 与项目宪法的架构/分层/命名约束。

***REMOVED******REMOVED*** 一、架构决策（最小集）

| 决策 | 选择 | 理由 |
|------|------|------|
| 同步/异步 | | |
| 事务策略 | | |
| 降级/兜底 | | |

***REMOVED******REMOVED*** 二、影响面确认（必做）

> 🔍 用代码检索确认波及范围，改动涉及的所有文件/模块/消息路线在此列出。

```
改动入口: {类.方法()}
  ├── 直接调用者:   (检索)
  ├── 级联影响 3 跳: (调用链)
  └── 跨模块消费者:  (检索 → 补漏)
```

| 波及点 | 类型（入口/消息/接口/DB） | 处置 |
|-------|------|------|
| | | |

***REMOVED******REMOVED*** 三、接口设计（最小集）

***REMOVED******REMOVED******REMOVED*** {Service}.{method}()

签名: `{ReturnType} {method}({Params})`

意图:
1.
2.

测试场景:
- {正常路径} → {预期结果}   [TDD / 端到端]
- {异常路径} → {预期结果}   [TDD]
- {边界条件} → {预期结果}   [TDD]

> 标注规则：内部逻辑分支 → `[TDD]`（单测）；涉及外部请求/端到端 → `[端到端]`；两者都涉及 → 两者都标。

***REMOVED******REMOVED*** 四、数据流（最小集）

```
入口 → 应用层 → 数据访问 → DB
                ↓
         外部调用 → 下游服务
```

***REMOVED******REMOVED*** 五、数据/结构变更（如涉及）

```sql
-- 新增表/字段
-- ALTER TABLE t_xxx ADD COLUMN yyy VARCHAR(64) DEFAULT '' COMMENT '说明';
```

> ⚠️ 结构变更必须先于代码部署（回滚条件）。

***REMOVED******REMOVED*** 六、关键代码模板（最小集）

```text
// 核心逻辑骨架（非完整代码）
```

***REMOVED******REMOVED*** 七、扩展（可选，按任务复杂度裁减）

| 节 | 适用场景 |
|----|---------|
| 安全条件验证方案 | 涉及计费/库存/权限 |
| 性能预算 | 涉及循环内外部调用 / 大数据量查询 |
| 兼容性矩阵 | 涉及对外 DTO / 消息体变更 |
| 回滚方案 | 涉及结构变更 |
| 灰度策略 | 涉及核心链路改动 |

***REMOVED******REMOVED*** 八、文档回写清单

| ***REMOVED*** | 文档 | 需要更新？ | 变更内容 |
|---|------|:--------:|---------|
| 1 | `laws.md` / 项目宪法 | | |
| 2 | 项目回溯目录 | | |
| 3 | 知识/记忆区 | | |
