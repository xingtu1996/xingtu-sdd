# XingTu SDD · 专业自述

> 定位：行途开源矩阵的规格驱动开发（SDD）方法论资产仓，核心主张——no specs, no code，先规格、后代码，聚合 Spec 模板、流程与案例。

## 1. 这个项目是干什么的（作用）

- **给谁用**：让 AI 写代码 / 做任务的工程师——尤其是被"AI 一次性给一堆不可预期的结果"坑过、想让产出变得可预期、可审计、可复现的人。
- **解决什么问题**：直接让 Agent 编码，结果随机、边界不清、验收靠感觉。SDD 把"复杂任务"拆成"先写规格再执行"，目标、约束、验收标准都先落在纸上，Agent 的行为从"自由发挥"变成"照着规格执行"。
- **核心价值**：
  1. **可复现**：同一任务按同一套 spec 流程，结果可预期，不再是抽卡。
  2. **可审计**：每一步有依据、有验证（01 分析 → 02 需求 → 03 设计 → 04 任务 → 05 验收），出问题能定位是哪一步的假设错了。
  3. **可复用**：方法论跨项目、跨团队通用；模板开箱即用，不用每次从零设计。
  4. **质量门禁前置**：验收标准（05_validator）在编码前就定义，Agent 知道自己"做到什么算完"，减少无限迭代。
  5. **蒸馏自真实实践**：spec 结构与流程来自真实项目的复盘，不是纸面理论。

## 2. 在行途 harness 中的定位

- **位置**：方法论层的可执行资产。harness 里 SDD 是"任务怎么被规格化地推进"的底层方法，向上支撑 workflows（编排多个任务），向下被 Coding Agent 在单个任务里执行。
- **协作关系**：

```
        ┌──────────────────────────────────────────────────┐
        │   xingtu-ai-engineering（方法论旗舰：SDD 理念/论述） │
        └───────────────────────┬──────────────────────────┘
                                │ 理念 → 落地
        ┌───────────────────────┴──────────────────────────┐
        │   xingtu-sdd（本仓）：specs/ 00-06 编号模板 + 案例   │
        │   00 README → 01 分析 → 02 需求 → 03 设计          │
        │   → 04 任务 → 05 验收 → 06 执行日志                 │
        └───────────────────────┬──────────────────────────┘
                                │ 被 Agent 按编号推进
        ┌───────────────────────┴──────────────────────────┐
        │   xingtu-workflow（多任务编排 / 生命周期）           │
        │   xingtu-rules（常驻约束） xingtu-skills（方法技能）  │
        └──────────────────────────────────────────────────┘
```

- **与相邻仓的关系**：ai-engineering 讲"SDD 是什么、为什么重要"，sdd 给"怎么一步步做"的可执行模板；rules 是常驻约束（永远生效），spec 是单任务规格（任务级、有生命周期）；workflow 管"多个任务怎么串并行"，spec 管"单个任务怎么规格化完成"。

## 3. 与其他项目的差异与区别

| 对比项 | xingtu-sdd | xingtu-ai-engineering | xingtu-rules | xingtu-workflow |
|--------|-----------|----------------------|--------------|-----------------|
| 关注对象 | 单个任务的规格化推进 | 方法论全景（理念/五大工程/治理） | 所有任务的常驻约束 | 多任务的生命周期与编排 |
| 粒度 | 任务级、临时 | 体系级、长期 | 工程级、常驻 | 流程级、可编排 |
| 产物形态 | specs/00-06 编号文件 | 方法论文档/模板 | 规则文件 | SOP / 流程文件 |
| 生命周期 | 随任务起止 | 长期演进 | 长期稳定 | 长期演进 |
| 何时选它 | 有个复杂任务要交给 Agent | 想系统学/设计整套方法论 | 想让 Agent 长期守规矩 | 想串起一批任务的执行顺序 |

一句话：**ai-engineering 是"理"，sdd 是"单个任务的法"，rules 是"常驻的规"，workflow 是"一串任务的序"**。

## 4. 在 Agent 体系中的应用

### 4.1 Work Agent（业务/内容工作流）

- **作用方法**：内容/调研/业务类复杂任务先写规格——01 分析背景与边界、02 明确需求与目标读者、05 定义可验证的完成标准，再让 Agent 按规格执行。
- **触发方法**：任务一开始就创建 `specs/<task>/` 目录，从模板复制 00_README 起步；Agent 按编号顺序推进，每步产出即验证。
- **典型场景**：多期内容选题（先规格化定位/受众/调性再产出）、调研报告（先定问题/范围/证据标准再收集）、运营活动（先定目标/约束/验收再执行）。

### 4.2 Coding Agent（编码 Agent，如 Claude Code）

- **作用方法**：复杂编码任务不再"一句话开干"，而是先落 spec：01 分析约束与既有代码边界，02 写需求规格，03 设计改动方案，04 拆任务，05 写验收标准，然后 Agent 照着实现并用 05 验证。
- **触发方法**：`cp specs/_TEMPLATE/00_README.md my-task/` 手动起规格；或由 agent 收到复杂任务时自动先走规格流程（规则/技能联动触发）。
- **典型场景**：跨多文件的重构、需要兼容旧行为的功能改动、有明确不可破坏约束的任务、需要事后审计"为什么这么做"的改动。

## 5. 升级方法与迭代开发

- **新增资产**：新增 spec 模板（新类型任务的标准流程）或真实案例（复盘后回填）。
- **演进路径**：从真实项目回溯，把"做得好的流程"蒸馏成新模板，把"翻车的原因"补进分析/验收环节——spec 本身也在做循环工程。
- **质量门禁**：
  - 模板必须保证 Agent 产出可审计的 00-06 全流程，缺任何一环视为不合格。
  - 验收标准（05）必须**可验证**（可运行检查 / 可测量），禁止"看起来合理"式的模糊验收。
  - 案例必须脱敏，只留方法论，不带具体业务数据。
- **当前状态**：`specs/_TEMPLATE` 与编号结构已就绪，模板填充与首批案例入库是当前主任务。

## 6. 基础概念

- **SDD（Spec-Driven Development，规格驱动开发）**：先写规格再写代码的开发方式，与 TDD（测试驱动）互补——TDD 用测试描述"行为"，SDD 用规格描述"目标、约束、验收"，两者都追求"先定义后实现"。
- **spec（规格）**：一份明确定义目标、约束、边界、验收标准的文档，是 Agent 任务的"契约"。
- **验收标准（acceptance criteria）**：判断任务"完成"的可验证条件。为什么重要：没有验收标准的任务，Agent 会在"做完"和"做对"之间无限漂移。
- **可复现性**：同一任务+同一规格 → 可预期结果。为什么重要：AI 的随机性必须被流程压制，否则团队无法依赖。
- **可审计性**：每一步有依据、可回溯，能回答"为什么这么做"。为什么重要：AI 的"自信错误"需要靠过程证据暴露。
- **规格生命周期**：创建（00）→ 分析（01）→ 需求（02）→ 设计（03）→ 拆解（04）→ 验收（05）→ 复盘归档（06）。
- **validator（验收执行）**：把 05 的验收标准变成可运行检查的环节，是质量门禁的落地点。

## 7. 专业背书

- "no specs, no code" 与 Spec-Driven Development 是业界真实方法论：Spawned AI 开源了配套 GitHub Copilot coding agent 的 spec-driven-development 工作流，GitHub 官方也在 coding agent 中推行 specs 先行的流程——本仓的"先规格后编码、验收前置"与其一脉相承。
- 编号化规格结构（背景/约束 → 需求 → 设计 → 任务 → 验收 → 日志）呼应软件工程里"需求→设计→实现→验证"的经典瀑布骨架，但压缩成单任务粒度、面向 Agent 执行，属于对经典方法在 AI 时代的重写。
- 验收前置与 TDD / BDD 的"先定义后实现"哲学一致；也与 Anthropic"Building effective agents"中强调的"可验证的验收条件 + 用反馈修正输出"的实践相符。
